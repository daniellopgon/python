b) 4 — len([1, 2, 3, 4]) cuenta los elementos.

a) [1, 2, 3] — .append(3) agrega al final.

b) insert() — inserta en un índice específico.

a) [1, 3] — elimina el valor en índice 1.

a) [0, 1, 2] — range(3) empieza en 0 y no incluye el 3.

a) 5 in list — verifica si 5 está presente.

c) Una sintaxis para generar listas en una línea.

a) [0, 1, 4] — 0², 1², 2².

b) [1, 2, 3] — sorted() ordena numéricamente.

c) index() — retorna la primera posición del valor.

a) [1, 2] — slicing desde el índice 0 hasta 2 (excluye 2).

a) 3 — accede al índice [1][0] → [3, 4][0].

d) Ambas — [:] y list() hacen copia.

c) remove() — elimina por valor.

b) Las enlaza al mismo objeto (referencia, no copia).

b) Inmutables — no se pueden modificar.

b) 2 — índice 1 en (1, 2, 3) es 2.

c) 2 — hay dos elementos.

a) (1,) — una tupla de un solo elemento necesita coma.

a) Sí — pueden contener listas u otros objetos.

b) ('a', 'b', 'c') — convierte cada carácter en un elemento.

a) (1, 2, 3, 4) — suma dos tuplas.

a) Sí — se pueden hacer slices igual que listas.

c) Las listas son mutables, las tuplas no.

b) int — sin coma, (5) es simplemente 5.

b) Pares clave-valor.

b) d[key] — así se accede a un valor.

c) Agrega clave 'b' con valor 2.

a) del d[key] — elimina una entrada.

b) d.keys() — retorna todas las claves.

a) "x" in d — comprueba si existe la clave.

c) Tuplas (clave, valor).

b) 2 — hay dos claves.

a) Sí — claves pueden ser cualquier tipo inmutable.

a) Sí — los valores pueden ser listas u objetos mutables.

b) 'b' — índice 1 en "abc" es 'b'.

c)

Hello  
World
\n es nueva línea.
38. b) 6 — tiene 6 caracteres.
39. b) Válida — comillas dobles no son necesarias si usas simples correctamente.
40. c) HelloWorld — concatenación directa.
41. a) ABC — .upper() convierte a mayúsculas.
42. d) Todas son válidas.
43. d) Ambas .find() y .index() retornan índice.
44. c) -1 — .find() retorna -1 si no encuentra.
45. b) Inmutables — las cadenas no se pueden modificar.
46. a) He--o — reemplaza todas las 'l' con '-'.
47. b) \t — representa tabulación.
48. a) True — 'a' está en "cat".
49. b) 'bc' — índices 1 y 2.
50. a) True — "Hello" empieza con "H".